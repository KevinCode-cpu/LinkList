import React from 'react';

const HeroImage = () => {
  return (
    <div className="relative w-full h-full">
      {/* This component is now intentionally simple to ensure no watermarks are present. 
          The actual hero image is handled via CSS/img tag in HomePage.jsx */}
    </div>
  );
};

export default HeroImage;